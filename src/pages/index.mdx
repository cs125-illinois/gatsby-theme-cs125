import { Single } from "../sites/"
export default Single

import { Lead } from "../material-ui/"
import { LoremIpsum } from "../lorem-ipsum/"

This page includes examples of a variety of elements that we use on CS 125 website.

## [`chitter`](/chitter)

## `jeed`

This example should run and include complexity highlights in the gutter.

```java id=java complexity
public class Main {
  public static void main() {
    System.out.println("Here");
  }
}
```

```java snippet
System.out.println("Java snippet!");
```

```java
public class Main {
  public static void main() {
    System.out.println("Java source!");
  }
}
```

```kotlin snippet
println("Kotlin snippet")
```

```kotlin
fun main() {
  println("Kotlin source")
}
```

```kotlin checkForSnippet
println("Should check for Kotlin snippet")
```

```java checkForSnippet
System.out.println("Should check for Java snippet");
```

## `mdx`

Testing some `mdx` and Material UI integrations.

<Lead>

This is lead text.

</Lead>

Let's try [a link](https://cs125.cs.illinois.edu).
And what about a [relative link](/second).

## `react-ace`

Install example. Should not use Jeed or `mace` (no `id`) but still look OK.

```sh id=install
// Install example
npm i @cs125/gatsby-theme-cs125
```

Styles for JSX examples.

```jsx
const App: React.FC = () => {
  return (
    <ElementTracker tags=["h1", "h2"]>
      <RestOfYourApp />
    </ElementTracker>
  )
}
```

## `mace`

These two editors should remain synchronized.

```java id=helloworld snippet
System.out.println("Here");
```

```java id=helloworld snippet
System.out.println("Here");
```

## `element-tracker`

Here's some dummy content to make the page long enough for element tracking to kick in.

<LoremIpsum p={8} />
